#!/bin/sh
set -e
# Automatically added by thin-edge.io
ACTION=
case "$0" in
    *.*-deinstall) ACTION=remove; ;;
esac

if [ "$ACTION" = remove ]; then
    if command -v tedgectl >/dev/null 2>&1; then
        tedgectl stop tedge-agent.service >/dev/null || true
    fi
fi
# End automatically added section